#!/usr/bin/env zsh

# Check if argument provided
if [ -z "$1" ]; then
    echo "Usage: $0 <username/repo>"
    echo "Example: $0 torvalds/linux"
    exit 1
fi

repo="$1"
args="$2"

# Clone the repository
git clone "https://github.com/${repo}.git" $2
